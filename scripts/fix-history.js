const fs=require("fs"); let s=fs.readFileSync("electron/core/agentLoop.js","utf8"); const old="messages.push({ role: \"assistant\", content: rawText })"; const rep="messages.push({ role: \"assistant\", content: historyContent })"; const count=(s.split(old).length-1); s=s.split(old).join(rep); fs.writeFileSync("electron/core/agentLoop.js",s,"utf8"); console.log("replaced "+count+" occurrences");