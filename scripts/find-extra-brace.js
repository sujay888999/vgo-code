const fs=require("fs"); const s=fs.readFileSync("electron/core/agentLoop.js","utf8"); const lines=s.split(/\r?\n/); let depth=0; for(let i=0;i<lines.length;i++){const l=lines[i]; for(const c of l){if(c==="{")depth++;if(c==="}") depth--;} if(depth<0){process.stdout.write("NEGATIVE at line "+(i+1)+": "+l+"\n"); break;} if(i>100&&i<160) process.stdout.write((i+1)+":"+depth+" "+l.slice(0,40)+"\n");}